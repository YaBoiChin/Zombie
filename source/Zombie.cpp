#include "Zombie.h"

void Zombie::Interact(typePerson) override {
    // if the current person is an Ignorant Person or Alarmed Person, turn them into a zombie
        if (typeid(typePerson) == typeid(Alarmed) || typeid(typePerson) == typeid(Ignorant)) {
            Zombie.push_back()
        }
    }